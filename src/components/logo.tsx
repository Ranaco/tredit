import { pacifico } from "./layout/main";
import * as React from "react";

const Logo: React.FC = () => {
  return <span className={`${pacifico.className} text-2xl`}>TE.</span>;
};

export default Logo;
